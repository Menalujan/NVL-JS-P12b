var numeros = [Math.floor((Math.random() * 100) + 1)];
var pares = [];
var impares = [];


function revisar(numeros){ 
    for(i = 0; i < 2; i++){
    if(numeros %2 == 0){
    push(pares);
    }else{
    push(impares);
    }
    return(pares)+(impares);
}
}